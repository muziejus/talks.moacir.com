<div class="reveal bg-washedBlue-500 font-serif text-columbiaBlue-200 text-base font-normal" {{reveal markdown=@markdownText }}>
  <div class="slides">
    {{#if @markdownText}}
      <section data-markdown>
        <label>
          <textarea data-template>
            {{@markdownText}} 
          </textarea>
        </label>
      </section>
    {{else}}
      {{yield}}
    {{/if}}
  </div>
</div>
